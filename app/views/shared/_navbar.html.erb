<nav id="header" class="w-full z-30 top-10 py-1 bg-white shadow-lg border-b border-teal-500">
  <div class="w-full flex items-center justify-between mt-0 px-6 py-2">
    <label for="menu-toggle" class="cursor-pointer md:hidden block">
      <svg class="fill-current text-blue-600" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20">
        <title>menu</title>
        <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
      </svg>
    </label>

    <input class="hidden" type="checkbox" id="menu-toggle">

    <div class="hidden md:flex md:items-center md:w-auto w-full order-3 md:order-1" id="menu">
      <nav>
        <ul class="md:flex items-center justify-between text-base text-teal-600 pt-4 md:pt-0">
          <li><%= link_to "Home", root_path, class: navbar_links %></li>
          <% if signed_in? %>

          <% end %>

        </ul>
      </nav>
    </div>

    <div class="order-2 md:order-3 flex flex-wrap items-center justify-end mr-0 md:mr-4" id="nav-content">
      <div class="auth flex items-center w-full md:w-full">
        <% if signed_in? %>
          <%= link_to current_user.first_name, user_path(current_user.id), class: "mr-5" %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: "false" }, class: "bg-gradient-to-b from-teal-600 to-green-500  hover:bg-gradient-to-l hover:from-teal-600 hover:to-green-500 text-gray-200  p-2 rounded hover:text-gray-100" %>
        <% else %>
          <%= render 'shared/dropdown' %>
          <%#= link_to "Log in", new_user_session_path, class: "bg-transparent text-gray-800  p-2 rounded border border-gray-300 mr-4 hover:bg-gray-100 hover:text-gray-700" %>
          <%= link_to "Sign up", new_user_registration_path, class: "ml-2 bg-gradient-to-b from-teal-600 to-green-500  hover:bg-gradient-to-l hover:from-teal-600 hover:to-green-500 text-gray-200  p-2 rounded hover:text-gray-100" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
