  <%= render "form", bank: @bank %>

  <div class="pt-4">
    <%= link_to t(:'banco.link_bancos'), banks_path, class: 'blue__btn' %>
    <%= link_to t(:'proveedores.redirigir', name: @bank.provider.name), provider_path(@bank.provider), class: 'blue__btn' %>
  </div>
